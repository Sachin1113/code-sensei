{
  "version": 2,
  "builds": [
    // REMOVE THE CLIENT BUILD ENTRY COMPLETELY
    /*
    {
      "src": "client/package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "npm run build",
        "outputDirectory": "dist",
        "rootDirectory": "client"
      }
    },
    */
    {
      "src": "api/generate.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/generate",
      "dest": "/api/generate.js",
      "methods": ["POST", "OPTIONS"]
    },
    {
      "src": "/(.*)",
      "dest": "/client/dist/$1" // Keep this route as it specifies where the client files *should* be
    }
  ]
}
